import type { ColumnDef } from '../../types';
import { TesseractStore } from './tesseractStore';
export interface SharedMemoryStoreOptions {
    id: string;
    segmentSize?: number;
    directory?: string;
    writable?: boolean;
}
export declare class SharedMemoryStore implements TesseractStore {
    readonly mode: 'array' | 'shared-memory';
    private readonly options;
    private readonly bendec;
    private readonly rowTypeName;
    private readonly sharedLog;
    private readonly writer;
    private readonly normalizedColumns;
    private readonly frameSize;
    private frames;
    get dataCache(): any[];
    constructor(columns: ColumnDef[], options: SharedMemoryStoreOptions);
    updateColumns(columns: ColumnDef[]): void;
    prepareForReset(): void;
    clear(): void;
    isRowInstance(candidate: any): boolean;
    createRowInstance(): any;
}
