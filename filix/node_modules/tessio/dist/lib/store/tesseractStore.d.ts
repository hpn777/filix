import { ColumnDef } from '../../types';
export interface TesseractStore {
    readonly mode: 'array' | 'shared-memory';
    dataCache: any[];
    createRowInstance(): any;
    isRowInstance(candidate: any): boolean;
    updateColumns(columns: ColumnDef[]): void;
    prepareForReset?(): void;
    clear?(): void;
}
export declare class DefaultTesseractStore implements TesseractStore {
    readonly mode: 'array' | 'shared-memory';
    dataCache: any[];
    private objDef;
    constructor(columns: ColumnDef[]);
    createRowInstance(): any;
    isRowInstance(candidate: any): boolean;
    updateColumns(columns: ColumnDef[]): void;
    prepareForReset(): void;
    clear(): void;
    private generateObjDef;
}
