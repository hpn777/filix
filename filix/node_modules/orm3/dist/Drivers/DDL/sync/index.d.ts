export type Callback<T = any> = (err?: Error | null, result?: T) => void;
export type DebugFn = (...args: any[]) => void;
export interface SyncOptions {
    driver: any;
    debug?: DebugFn | false;
    suppressColumnDrop?: boolean;
}
interface TypeDefinition {
    datastoreType: (property: Record<string, any>) => any;
}
type DialectModule = Record<string, any>;
export declare class Sync {
    private readonly debug;
    private readonly driver;
    private readonly dialect;
    private readonly suppressColumnDrop;
    private readonly collections;
    private readonly types;
    private totalChanges;
    constructor(options: SyncOptions);
    defineCollection(collection: string, properties: Record<string, any>): this;
    defineType(type: string, definition: TypeDefinition): this;
    sync(cb: Callback<{
        changes: number;
    }>): void;
    private processCollection;
    private createCollection;
    private createColumn;
    private syncIndexes;
    private getCollectionIndexes;
    private getIndexName;
    private needToSync;
    private syncCollection;
}
export declare const dialect: (name: string) => DialectModule;
declare const _default: {
    Sync: typeof Sync;
    dialect: (name: string) => DialectModule;
};
export default _default;
//# sourceMappingURL=index.d.ts.map