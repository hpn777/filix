import { Table } from '../table';
import { SqliteColumn } from './column';
import { SqliteIndex } from './index';
interface SqliteDriverLike {
    execSimpleQuery(sql: string): Promise<unknown>;
    query: {
        escapeVal(val: unknown, timezone?: string): string;
    };
}
export declare class SqliteMetadataDriver {
    private readonly driver;
    constructor(driver: SqliteDriverLike);
    getVersion(): Promise<string | undefined>;
    getTables(): Promise<Table[]>;
    getColumns(tableName: string): Promise<SqliteColumn[]>;
    getIndexes(tableName: string): Promise<SqliteIndex[]>;
    close(): Promise<void>;
    private escape;
    private exec;
    private getUniqueColumnNames;
}
export {};
//# sourceMappingURL=metadata.d.ts.map