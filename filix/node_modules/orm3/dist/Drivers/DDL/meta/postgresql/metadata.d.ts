import { Table } from '../table';
import { PgColumn } from './column';
import { PgIndex } from './index';
interface PostgresDriverLike {
    execSimpleQuery(sql: string): Promise<unknown>;
    query: {
        escapeVal(val: unknown, timezone?: string): string;
    };
    config?: {
        schema?: string;
    };
}
export declare class PostgresMetadataDriver {
    private readonly driver;
    private readonly schemaOverride?;
    constructor(driver: PostgresDriverLike, schemaOverride?: string | undefined);
    getVersion(): Promise<string | undefined>;
    getTables(): Promise<Table[]>;
    getColumns(tableName: string): Promise<PgColumn[]>;
    getIndexes(tableName: string): Promise<PgIndex[]>;
    close(): Promise<void>;
    private escape;
    private escapeSchema;
    private exec;
    private getSchema;
}
export {};
//# sourceMappingURL=metadata.d.ts.map