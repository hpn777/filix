"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.number = number;
exports.length = length;
const Validator_1 = __importDefault(require("../Validator"));
function number(min, max, message = 'out-of-range-number') {
    return new Validator_1.default((value, next) => {
        if (value === undefined || value === null)
            return next('undefined');
        const numeric = value;
        if (min === undefined && max !== undefined && numeric <= max)
            return next();
        if (max === undefined && min !== undefined && numeric >= min)
            return next();
        if (min !== undefined && max !== undefined && numeric >= min && numeric <= max)
            return next();
        return next(message);
    });
}
function length(min, max, message = 'out-of-range-length') {
    return new Validator_1.default((value, next) => {
        if (value === undefined || value === null)
            return next('undefined');
        const len = value.length;
        if (min === undefined && max !== undefined && len <= max)
            return next();
        if (max === undefined && min !== undefined && len >= min)
            return next();
        if (min !== undefined && max !== undefined && len >= min && len <= max)
            return next();
        return next(message);
    });
}
//# sourceMappingURL=ranges.js.map