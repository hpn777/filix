{
  "name": "filix-app",
  "version": "0.1.0",
  "private": true,
  "description": "Application service powered by the filix backend module.",
  "scripts": {
    "prestart": "npm run build",
    "start": "node dist/server.js",
    "start:dev": "ts-node src/server.ts",
    "build": "tsc"
  },
  "dependencies": {
    "filix": "file:../filix"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
